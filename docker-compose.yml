services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - .:/app
    command: python -m flask --app main run --host=0.0.0.0 --port=8080 --debug
    restart: unless-stopped
